#防火墙预留自定义ssrplus端口规则
sed -i '/REDIRECT --to-ports 1234/d' /etc/firewall.user
echo '#(sleep 5s; iptables -t nat -A SS_SPEC_WAN_FW -p tcp -m multiport --dport 85,86 -j REDIRECT --to-ports 1234)&' >> /etc/firewall.user
